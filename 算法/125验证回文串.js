/**
 * @param {string} s
 * @return {boolean}
 */
var isPalindrome = function(s) {
    //验证时不考虑字母的大小写    
    //只考虑字母和数字字符

    //标点符号也不计入

    


    
};

var str = "A man, a plan, a canal: Panama"
var val = isPalindrome(str)
console.log('val: ', val);