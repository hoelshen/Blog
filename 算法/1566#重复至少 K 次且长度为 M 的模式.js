/**
 * @param {number[]} arr
 * @param {number} m
 * @param {number} k
 * @return {boolean}
 */
 var containsPattern = function(arr, m, k) {
  // 处理m个模式，k次
  dp[0] = 1;
  for(let i = 0; i< arr.length; i++){
    // 模式 是由一个或多个值组成的子数组（连续的子序列） 这里的模式代表的意思是：长度为m的连续子数组
    // 有动态规划得出
    
  }
};